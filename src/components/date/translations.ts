import { ITranslations } from '../util/Translate';
const translations: ITranslations = {
    "default": {
        "full.date": "{day} the {date} of {month}"
    },
    "sv-SE": {
        "full.date": "{day} den {date} {month}"
    }
}

export default translations